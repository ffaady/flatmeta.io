import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.leaflet.example',
  appName: 'Flatmeta',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
